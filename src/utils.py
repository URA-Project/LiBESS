from all_packages import *


def load_data(path) -> pd.DataFrame:
    assert osp.isfile(path)

    df = None

    ## Đọc file excel

    return df


battery_type = {
    "JSU": "000",
    "LEK": "001",
    "NAJ": "010",
    "POW": "011",
    "ZSF": "100",
}

device = {
    # add device...
}
